import { NumberSplitPipe } from './number-split.pipe';

describe('NumberSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
